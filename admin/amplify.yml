version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn install
            - amplify pull --appId drvgi9rf8ppjo --envName $AWS_BRANCH --y
            - rm -Rf amplify/
            - ls -la
        build:
          commands:
            - yarn run build
            - cat src/aws-exports.js
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: admin